export interface Tasks {
    id: string;
    userId: string;
    title: string;
    summary:string;
    dueDate: string;
  }

export interface newTaskData {
  title: string;
  summary: string;
  date: string;
}